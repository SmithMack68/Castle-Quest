import React from 'react'
import Castle from './Castle'

const Castles = ({ castles }) => {
  return (
    <div>Castle</div>
    //{castles.map((castle) =>(
     // <Castle key={} castle={ castle } />
    // ))}
  )
}

export default Castles